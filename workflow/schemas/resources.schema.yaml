$schema: "http://json-schema.org/draft-04/schema#"
description: snakemake configuration file
type: object
properties:
  default_resources:
    type: object
    properties:
      threads:
        type: integer
        description: default number of threads that will be used by a rule
      time:
        type: string
        description: default max execution time for a rule
      mem_mb:
        type: integer
        description: default memory used for a rule
      mem_per_cpu:
        type: integer
        description: default memory used per cpu for a rule
      partition:
        type: string
        description: default partition to use on the cluser for a rule

  required:
    - default_resources

  filter_vcf_on_format:
    type: object
    properties:
      threads:
        type: integer
        description: number of threads that will be used by 'filter_vcf_on_format'
      time:
        type: string
        description: max execution time for 'filter_vcf_on_format'
      mem_mb:
        type: integer
        description: memory used for 'filter_vcf_on_format'
      mem_per_cpu:
        type: integer
        description: memory used per cpu for 'filter_vcf_on_format'
      partition:
        type: string
        description: partition to use on the cluster for 'filter_vcf_on_format'

  artifact_filter:
    type: object
    properties:
      threads:
        type: integer
        description: number of threads that will be used by 'artifact_filter'
      time:
        type: string
        description: max execution time for 'artifact_filter'
      mem_mb:
        type: integer
        description: memory used for 'artifact_filter'
      mem_per_cpu:
        type: integer
        description: memory used per cpu for 'artifact_filter'
      partition:
        type: string
        description: partition to use on the cluster for 'artifact_filter'

  background_filter:
    type: object
    properties:
      threads:
        type: integer
        description: number of threads that will be used by 'background_filter'
      time:
        type: string
        description: max execution time for 'background_filter'
      mem_mb:
        type: integer
        description: memory used for 'background_filter'
      mem_per_cpu:
        type: integer
        description: memory used per cpu for 'background_filter'
      partition:
        type: string
        description: partition to use on the cluster for 'background_filter'

  add_multi_snv_in_codon:
    type: object
    properties:
      threads:
        type: integer
        description: number of threads that will be used by 'add_multi_snv_in_codon'
      time:
        type: string
        description: max execution time for 'add_multi_snv_in_codon'
      mem_mb:
        type: integer
        description: memory used for 'add_multi_snv_in_codon'
      mem_per_cpu:
        type: integer
        description: memory used per cpu for 'add_multi_snv_in_codon'
      partition:
        type: string
        description: partition to use on the cluster for 'add_multi_snv_in_codon'
